import 'package:flutter/material.dart';

class ViewLeaveRequestsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Leave Requests'),
      ),
      body: Center(
        child: Text('View Leave Requests Page'),
      ),
    );
  }
}
