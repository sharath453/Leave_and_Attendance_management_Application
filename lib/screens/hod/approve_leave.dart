import 'package:flutter/material.dart';

class ApproveLeavePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Approve Leave Requests'),
      ),
      body: Center(
        child: Text('Approve Leave Requests Page'),
      ),
    );
  }
}
